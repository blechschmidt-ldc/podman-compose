# Decoy: would be picked up if mid's nested `project_directory: inner_alt`
# were resolved against mid/ instead of against mid's actual project_directory
# (top_alt/). Catches narrow-semantics regressions.
NESTED_EXPLICIT_VAR=wrong-from-mid-inner-alt
